# Dashboard integration

Import:

```tsx
import { UnbreakableDashboard } from "@/components/sovereign/malik-unbreakable-ai"
```

Use:

```tsx
<UnbreakableDashboard onPrompt={(prompt) => handleSendMessage(prompt)} />
```

Best: add it as sidebar view called "Unbreakable AI".
